<!DOCTYPE html>
<html>
<head>
    <title>title</title>
    <link rel="stylesheet" href="/style.css" />
</head>
<body>
    <div class="nav-bar">
        <ul><li><a href="/home">Home</a></li></ul>
        <ul><li><a href="/newblog">New Blog</a></li></ul>
        <ul><li><a href="/about">About</a></li></ul>
    </div>
    <div class="content">
        <h1>Blog Details</h1>
        <div class="blog">
            <div class="blog-post">
                <h2><%= blog.title %></h2>
                <p><%= blog.content %></p>
                <p>Author: <%= blog.auth_name %></p>
            </div>
        </div>
        <h3>Comments</h3>
        <div class="comments">
            <% if (comments && comments.length > 0) { %>
                <% comments.forEach(comment => { %>
                    <div class="comment">
                        <h4>UserName:- <%= comment.auth_name %></h4>
                        <h4><%= comment.comment %></h4>
                    </div>
                <% }) %>
            <% } else { %>
                <p>No comments yet</p>
            <% } %>
        </div>
        <h3>Add Comment</h3>
        <form class="form-new" action="/blog/<%= blog.id %>/comment" method="post">
            <label for="author">UserName:</label>
            <input type="text" id="author" name="author" required>
            <br>
            <label for="content">Comment:</label>
            <textarea id="content" name="content" required></textarea>
            <br>
            <button type="submit">Add Comment</button>
        </form>
    </div>
    <footer style="text-align: center;">
        <p>&copy; 2022 Bhaskar Blog Post. All rights reserved.</p>
    </footer>
</body>
</html>
